[package]
name = "mostro-score-web"
version = "0.1.0"
license = "GPL-3.0-only"
edition = "2021"

[dependencies]
axum = "0.8.8"
tokio = { version = "1", features = ["full"] }
tower-http = { version = "0.6", features = ["fs", "cors"] }
